# 上游 kit 来源。PixelML 配方没有公开 git 仓库（含 build/ 补丁）。
# 生产：走 blade-resource-keeper 物料 qwen38-sglang-kit（解到本目录 upstream/，fetch-upstream 视为已拉取；
#       .sglang-api-key 不在物料里，装机后单独下发到 upstream/.sglang-api-key），或装机时 KIT_TAR= 指向包内 tar。
# SEED 仅开发机兜底。
REPO=
REF=
KIT_TAR=
SEED=ai@192.168.130.8:/opt/qwen38-sglang/
